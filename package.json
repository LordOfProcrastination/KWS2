{
  "devDependencies": {
    "husky": "^9.0.11",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vite": "^5.2.8"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm test && vite build",
    "check": "prettier --check . && tsc --noEmit",
    "prepare": "husky",
    "test": "prettier --check . && tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
